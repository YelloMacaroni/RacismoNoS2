using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Photon.Pun;

public class LampeSyncro : MonoBehaviourPunCallbacks
{
    /*
    [SerializeField] public GameObject FlashLight;
    // Update is called once per frame

  
    void Update()
    {
        
        if (FlashLight.transform.localScale != new Vector3(0.001f,0.001f,0.001f))

            {Debug.Log("allumer");
                FlashLight.GetComponentInChildren<Light>().enabled = true;
            FlashLight.SetActive(true);}
        else{
            Debug.Log("Eteindre");
            FlashLight.GetComponentInChildren<Light>().enabled = false;
            FlashLight.SetActive(false);
        }
    }
    */
    
}
